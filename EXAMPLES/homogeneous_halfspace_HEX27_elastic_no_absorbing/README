----------------------------------------------------------------------
README
----------------------------------------------------------------------

This example creates a homogeneous halfspace, i.e. a single volume block with a
constant elastic material property and runs a forward simulation. The mesh uses
27-node elements.

To run this example, type:

> ./run_this_example.sh


