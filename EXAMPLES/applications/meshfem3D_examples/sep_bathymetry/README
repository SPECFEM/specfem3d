----------------------------------------------------------------------
README
----------------------------------------------------------------------

Marine setup example:
  uses a velocity model in SEP-format 
  (water layer on top, 3D velocity model with fast-velocity layer in middle)


To run example, type: 

 > run_this_example.sh 

from local directory (must have access to mpi)

